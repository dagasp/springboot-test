apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: my-cluster
  annotations:
    backstage.io/kubernetes-id: 'mini-kube'
spec:
  type: kubernetes-cluster
  lifecycle: production
  owner: user:guest
